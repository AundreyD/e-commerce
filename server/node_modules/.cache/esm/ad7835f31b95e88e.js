let express,Product;_1b9‍.x([["productsRoutes",()=>productsRoutes]]);_1b9‍.w("express",[["default",["express"],function(v){express=v}]]);_1b9‍.w("../models/product",[["Product",["Product"],function(v){Product=v}]]);


const router = express.Router();

router.get(`/`, async (req, res) => {
    // const product = Product(1, 'cabinet', 'img url')
    const productList = await Product.find()
    if(!productList) {
        res.status(500).json({success: false})
    }
    res.send(productList)
})

router.post(`/`, (req, res) => {
    const newProduct = req.body;
    _1b9‍.g.console.log(newProduct)
    res.send(newProduct)
})

       const productsRoutes = router;